
  # Create UI from File

  This is a code bundle for Create UI from File. The original project is available at https://www.figma.com/design/NyzZv0c65Bwo2fiGZF1kIC/Create-UI-from-File.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  